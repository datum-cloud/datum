apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: resource-manager-validation
  labels:
    app.kubernetes.io/name: datum
    app.kubernetes.io/component: resource-manager
    app.kubernetes.io/part-of: resource-manager-service

resources:
  - organization-update-policy.yaml

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/name: datum
      app.kubernetes.io/component: resource-manager
