# This kustomization is used as an entrypoint for loading all of the service
# configurations from the repo.
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

components:
  - resourcemanager.miloapis.com/
